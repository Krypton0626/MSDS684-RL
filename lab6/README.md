# Lab 6 — Policy Gradient Methods on CartPole

This lab implements policy gradient methods (REINFORCE) on
the CartPole-v1 environment using PyTorch.

The goal is to compare vanilla REINFORCE and REINFORCE
with a learned value-function baseline.